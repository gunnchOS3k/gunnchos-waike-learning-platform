# PR1 historical gate — superseded as authoritative CI by `.github/workflows/pr2.yml` after Wave 2.
# Kept for manual re-runs of the PR1 foundation matrix only.
name: PR1

on:
  workflow_dispatch:

env:
  SOURCE_DATE_EPOCH: "1700000000"
  WAIKE_DEV_DB_KEY: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  CARGO_TERM_COLOR: always
  WAIKE_PIN_REF: "fbf7685bc5686201ccaa0128ee83346d59b3d584"

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "PR1 workflow is workflow_dispatch-only."
          echo "Authoritative Learning OS CI for Wave 2+ is .github/workflows/pr2.yml"
          exit 0
